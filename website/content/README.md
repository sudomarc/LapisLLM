# Website content

This directory is the structured source layer for the Lapis platform site. `data.js` carries the UI data model; the Markdown files below document the corresponding domains. Keep published claims aligned with repository evidence.