# Evaluation

Lapis uses evidence gates rather than capability assumptions. The development sequence is correctness, tiny-dataset learning, held-out loss/perplexity, generation regression, then scaling.

The repository includes evaluation and benchmark developer commands, but broad benchmark results are not currently published. The website therefore does not assign scores, rankings, throughput claims, or qualitative capability claims to Lapis.
