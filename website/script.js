(()=>{
const D=window.LAPIS_DATA||{},body=document.body,app=document.querySelector('#app');const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)],esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const base=body.dataset.base||'./',view=body.dataset.view||'/';const link=(p='')=>base+p;
const pill=s=>`<span class="pill">${esc(s)}</span>`;
const nav=[['models/','Models'],['skills/','Skills'],['docs/','Docs'],['research/','Research'],['roadmap/','Roadmap']];
function header(){return `<header class="site-head"><div class="shell nav"><a class="brand" href="${link()}"><span class="mark">L</span><span>Lapis</span><span class="suffix">LLM</span></a><nav class="navlinks" aria-label="Primary">${nav.map(([p,t])=>`<a class="${view.startsWith('/'+p.split('/')[0])?'active':''}" href="${link(p)}">${t}</a>`).join('')}</nav><div class="navright"><button class="search-trigger" type="button" data-search-open>⌘K <span>Search</span></button><a class="git" href="https://github.com/sudomarc/LapisLLM" target="_blank" rel="noreferrer">GitHub ↗</a><button class="menu" type="button" aria-label="Open navigation" aria-expanded="false" data-menu>☰</button></div></div><div class="mobile" data-mobile><div class="shell">${nav.map(([p,t])=>`<a href="${link(p)}">${t}</a>`).join('')}<a href="${link('docs/')}">Get Started</a></div></div></header>`}
function footer(){return `<footer class="footer"><div class="shell footgrid"><div><a class="brand" href="${link()}"><span class="mark">L</span><span>Lapis</span><span class="suffix">LLM</span></a><p class="note">Open-source model engineering, documentation, and developer tooling.</p></div><div><span class="label">Explore</span><a href="${link('models/')}">Models</a><a href="${link('skills/')}">Skills</a><a href="${link('docs/')}">Docs</a></div><div><span class="label">Project</span><a href="${link('research/')}">Research</a><a href="${link('roadmap/')}">Roadmap</a><a href="${link('changelog/')}">Changelog</a></div><div><span class="label">Source</span><a href="https://github.com/sudomarc/LapisLLM" target="_blank" rel="noreferrer">GitHub ↗</a><a href="${link('about/')}">About</a></div></div><div class="shell bottom"><span>© 2026 LapisLLM</span><span>Built for inspectability.</span></div></footer>`}
const shell=h=>header()+`<main id="main">${h}</main>`+footer()+`<div id="search-root"></div>`;
function page(title,kicker,desc){return `<section class="shell page"><div class="eyebrow">${esc(kicker)}</div><h1>${esc(title)}</h1><p class="lede">${esc(desc)}</p></section>`}
function home(){return `<section class="hero shell"><div><div class="eyebrow">● LAPIS ${D.version} · CORRECTNESS</div><h1>Open-source reasoning models for developers.</h1><p class="lede">Lapis is a from-scratch decoder-only Transformer project that keeps the model stack visible: tokenizer, data pipeline, training, evaluation, inference, and developer tooling.</p><div class="actions"><a class="btn primary" href="${link('docs/')}">Get Started →</a><a class="btn ghost" href="https://github.com/sudomarc/LapisLLM" target="_blank" rel="noreferrer">View on GitHub ↗</a></div><div class="meta"><span>Python + PyTorch</span><span>MIT licensed</span><span>CPU-friendly development</span></div></div><div class="hero-art" role="img" aria-label="Lapis technical pipeline"><div class="grid"></div><div class="node n1"><small>01</small><b>TEXT</b><small>raw input</small></div><div class="line l1"></div><div class="node n2"><small>02</small><b>TOKENS</b><small>ByteLevel BPE</small></div><div class="line l2"></div><div class="core"><b>L</b><span>TRANSFORMER</span></div><div class="line l3"></div><div class="node n3"><small>03</small><b>LOSS</b><small>causal objective</small></div><div class="line l4"></div><div class="node n4"><small>04</small><b>OUTPUT</b><small>next-token logits</small></div><div class="caption"><span>Inspectable path</span><code>data → tokenize → train → evaluate → infer</code></div></div></section>
<section class="shell section"><div class="twocol"><div class="idx">01 / SYSTEM</div><div><h2>Build the stack.<br><em>Measure what it does.</em></h2><p>Lapis is deliberately small at the current stage. The repository exposes the mechanics of a language model instead of hiding them behind a hosted endpoint or high-level training framework.</p><div class="links"><a href="${link('research/')}">Read the research →</a><a href="${link('about/')}">Project principles →</a></div></div></div></section>
<section class="shell section"><div class="headrow"><div><div class="idx">02 / MODELS</div><h2>One architecture.<br><em>Multiple scales.</em></h2></div><a class="plain" href="${link('models/')}">View all models →</a></div><div class="cards">${D.models.slice(0,3).map(m=>`<article class="card"><div class="cardtop">${pill(m.status)}<span class="mono">${esc(m.name)}</span></div><h3>${esc(m.name)}</h3><p>${esc(m.role)}</p><div class="mini"><div><span>Config</span><b>${esc(m.config)}</b></div><div><span>Benchmarks</span><b>Not yet published</b></div></div></article>`).join('')}</div></section>
<section class="band"><section class="shell section"><div class="headrow"><div><div class="idx">03 / SKILLS</div><h2>A developer layer<br><em>around the model.</em></h2></div><a class="plain" href="${link('skills/')}">Explore skills →</a></div><div class="skilllist">${D.skills.slice(0,5).map(s=>`<a class="skillrow" href="${link('skills/'+s.slug+'/')}"><span class="mark">${s.category.slice(0,1)}</span><span><b>${esc(s.name)}</b><small>${esc(s.description)}</small></span><span class="arrow">→</span></a>`).join('')}</div></section></section>
<section class="shell section"><div class="headrow"><div><div class="idx">04 / DEVELOPER EXPERIENCE</div><h2>Use Lapis where<br><em>you build.</em></h2></div></div><div class="dev"><article class="card"><span class="eyebrow">PYTHON</span><h3>Train from the CLI.</h3><p>Use the repository entry points to prepare data, train, evaluate, generate, chat, and serve.</p><code>python scripts/train.py --config configs/tiny.yaml</code></article><article class="card"><span class="eyebrow">LOCAL</span><h3>Keep experiments close.</h3><p>The tiny configuration is designed for development on a small machine; larger training is a future step.</p><div class="stats"><div class="stat"><b>CPU</b><span>supported</span></div><div class="stat"><b>YAML</b><span>configured</span></div><div class="stat"><b>CI</b><span>tested</span></div></div></article><article class="card"><span class="eyebrow">API</span><h3>Design for integration.</h3><p>A FastAPI layer exists in the repository and is being evolved toward a stable developer-facing contract.</p><div class="stats"><div class="stat"><b>REST</b><span>direction</span></div><div class="stat"><b>Stream</b><span>direction</span></div><div class="stat"><b>Local</b><span>first</span></div></div></article></div></section>
<section class="shell section"><div class="headrow"><div><div class="idx">05 / EVALUATION</div><h2>Evidence before<br><em>capability claims.</em></h2></div><a class="plain" href="${link('research/evaluation/')}">Methodology →</a></div><div class="bench"><div><span class="label">Published results</span><strong>Not yet benchmarked</strong><p>Numbers will be added only from reproducible experiments.</p></div><div class="benchgrid">${['MMLU','GSM8K','HumanEval','ARC','TruthfulQA','Tokens/sec'].map(x=>`<span>${x}<b>—</b></span>`).join('')}</div></div></section>
<section class="shell section"><div class="twocol"><div><div class="idx">06 / RESEARCH</div><h2>Document the machine,<br><em>not just the interface.</em></h2></div><div class="researchlist">${D.research.map((r,i)=>`<a class="researchitem" href="${link('research/'+r.slug+'/')}"><span class="idx">${String(i+1).padStart(2,'0')}</span><div><h2>${esc(r.name)}</h2><p>${esc(r.description)}</p></div><span>→</span></a>`).join('')}</div></div></section>
<section class="shell section"><div class="headrow"><div><div class="idx">07 / ROADMAP</div><h2>Correctness → validation →<br><em>scale.</em></h2></div><a class="plain" href="${link('roadmap/')}">Read roadmap →</a></div><div class="timeline"><div class="time"><span class="idx">01</span><b>Foundation</b><small>Completed</small></div><div class="time"><span class="idx">02</span><b>Validation</b><small>In progress</small></div><div class="time"><span class="idx">03</span><b>Small model</b><small>Planned</small></div><div class="time"><span class="idx">04</span><b>Scale</b><small>Exploring</small></div></div></section>
<section class="shell final"><div class="finalbox"><div><div class="eyebrow">BUILD WITH LAPIS</div><h2>The model is the beginning.</h2><p>Follow implementation, experiments, skills, documentation, and releases in the open.</p></div><a class="btn primary" href="${link('docs/')}">Start with the docs →</a></div></section>`}
function modelsPage(){return page('MODEL CATALOG','MODELS','Models built with evidence gates. Released, experimental, and planned configurations are shown separately. Missing data stays explicitly unpublished.')+`<section class="shell section"><div class="notice"><strong>Current state</strong>Lapis ${D.version} is a correctness-focused development release; it is not yet a competitive pretrained foundation model.</div>${D.models.map(m=>`<article class="modelrow"><div class="modelmain"><div class="cardtop">${pill(m.status)}<span class="mono">${esc(m.name)}</span></div><h2>${esc(m.name)}</h2><p>${esc(m.role)}</p><div class="specs"><div class="spec"><span>Status</span><b>${esc(m.status)}</b></div><div class="spec"><span>Architecture</span><b>${esc(m.architecture)}</b></div><div class="spec"><span>Context</span><b>${m.name==='Lapis Tiny'?'128 tokens':'Not yet published'}</b></div><div class="spec"><span>Parameters</span><b>Not published</b></div><div class="spec"><span>License</span><b>${esc(m.license)}</b></div><div class="spec"><span>Memory</span><b>Not yet published</b></div></div></div><div class="aside"><label>Intended use</label><p>${esc(m.role)}</p><label>Inference</label><p>${m.name==='Lapis Tiny'?'Local generation and serving scripts are available in the repository.':'Not yet published'}</p><label>Release</label><p>${m.name==='Lapis Tiny'?'Experimental development configuration':'Planned / unpublished'}</p><label>Benchmarks</label><p>Not yet published</p></div></article>`).join('')}</section>`}
function skillsPage(){return page('LAPIS SKILLS','SKILLS','Reusable workflows for the AI engineering stack. A Lapis-native skill is a versioned package with explicit purpose, inputs, outputs, workflow, limitations, and safety boundaries.')+`<section class="shell section"><div class="searchbar"><label class="input">⌕<input type="search" placeholder="Search skills" aria-label="Search skills" data-skill-search></label><div class="filters">${['All','Development','Security','Research','Documentation','Data','Evaluation','Productivity'].map((x,i)=>`<button class="filter ${i===0?'on':''}" type="button" data-filter="${x==='All'?'all':x}">${x}</button>`).join('')}</div></div><div class="market">${D.skills.map(s=>`<a data-skill="${s.category}" data-text="${(s.name+' '+s.category+' '+s.description).toLowerCase()}" href="${link('skills/'+s.slug+'/')}" class="skill-card-link"><article class="card"><div class="cardtop"><span class="mark">${s.category[0]}</span>${pill(s.status)}</div><h3>${esc(s.name)}</h3><p>${esc(s.description)}</p><div class="meta"><span>${esc(s.category)}</span><span>v${esc(s.version)}</span><span>by Lapis</span><span>★ —</span></div></article></a>`).join('')}</div><p data-empty hidden>No skills match this filter.</p><section class="section"><div class="twocol"><div><div class="idx">SPECIFICATION</div><h2>SKILL.md is the contract.</h2></div><div><p>Skills stay close to code: reviewable, versioned, auditable, and explicit about safety boundaries.</p><a class="plain" href="https://github.com/sudomarc/LapisLLM/tree/main/website/content/skills" target="_blank" rel="noreferrer">Browse source ↗</a></div></div></section></section>`}
function docsPage(){return page('DOCUMENTATION','DOCS','A developer-first guide to installing, training, evaluating, serving, and extending Lapis.')+`<section class="shell section"><div class="docs-home"><div><div class="idx">START HERE</div><h2>Build the model, then understand each layer.</h2><p>The docs reflect the current repository rather than an imagined production stack. Commands are concrete; missing features stay marked.</p></div><div class="docsgrid">${D.docs.map((d,i)=>`<a class="docbox" href="${link('docs/'+d.slug+'/')}"><span class="idx">${String(i+1).padStart(2,'0')}</span><h3>${esc(d.name)}</h3><p>${esc(d.description)}</p><span class="plain">Read →</span></a>`).join('')}</div></div></section>`}
function docDetail(slug){const d=D.docs.find(x=>x.slug===slug);if(!d)return notFound();const code={'getting-started':'git clone https://github.com/sudomarc/LapisLLM.git\ncd LapisLLM\npython -m venv .venv\nsource .venv/bin/activate\npip install -e .\npython -m pytest\npython scripts/train.py --config configs/tiny.yaml','models':'# Development configuration\npython scripts/train.py --config configs/tiny.yaml','development':'python -m pytest\nruff check .\npython scripts/evaluate.py --checkpoint checkpoints/latest.pt --device cpu','training':'python scripts/train.py --config configs/tiny.yaml\npython scripts/train.py --config configs/tiny.yaml --resume checkpoints/latest.pt','skills':'---\nname: example-skill\ndescription: One-line purpose\nversion: 0.1.0\n---\n\n# Example Skill\n## Purpose\n## Inputs\n## Outputs\n## Workflow\n## Limitations\n## Safety','reference':'cat configs/tiny.yaml\npython scripts/train.py --config configs/tiny.yaml\npython scripts/evaluate.py --checkpoint checkpoints/latest.pt --device cpu'}[slug]||'# See the repository source of truth.';return `<section class="shell page"><div class="eyebrow">DOCUMENTATION / ${esc(d.name)}</div><h1>${esc(d.name)}</h1><p class="lede">${esc(d.description)}</p></section><section class="shell doclayout"><aside class="sidebar"><div class="side-title label">Docs</div>${D.docs.map(x=>`<a href="${link('docs/'+x.slug+'/')}"><b>${esc(x.name)}</b><small>${esc(x.description)}</small></a>`).join('')}</aside><article class="article"><div class="idx">OVERVIEW</div><h2>${esc(d.name)}</h2><p>${esc(d.description)}</p><h3>Core notes</h3><ul><li>Keep implementation and documentation aligned.</li><li>Distinguish current behavior from planned work.</li><li>Prefer reproducible commands and explicit limitations.</li><li>Validate changes with the repository test and lint checks.</li></ul><h3 id="example">Example</h3><div class="codewrap"><button class="copy" type="button" data-copy>Copy</button><pre class="code"><code>${esc(code)}</code></pre></div><div class="links"><a href="${link('docs/')}">← Docs home</a><a href="${link('docs/')}">Docs index →</a></div></article><aside class="toc"><div class="label">On this page</div><a href="#main">Overview</a><a href="#example">Example</a></aside></section>`}
function diagramSvg(slug){const labels={architecture:['Tokens','Embedding','RMSNorm','GQA + RoPE','SwiGLU MLP','LM head'],tokenizer:['Text','ByteLevel BPE','Token IDs','Packed sequences','Training batches'],training:['Dataset','Tokenizer','Batches','Forward pass','Loss','Backpropagation','Optimizer','Checkpoint'],evaluation:['Model','Benchmark suite','Metrics','Analysis','Report'],limitations:['Correctness','Validation','Scale','Benchmarks','Production readiness']}[slug]||['Step 1','Step 2','Step 3'];const h=90+labels.length*60;return `<svg class="diagram-svg" viewBox="0 0 820 ${h}" role="img" aria-label="${esc(labels.join(' to '))}"><defs><marker id="arrow" viewBox="0 0 10 10" refX="5" refY="5" markerWidth="5" markerHeight="5" orient="auto-start-reverse"><path d="M0 0L10 5L0 10z" fill="#aeb8c4"/></marker></defs>${labels.map((t,i)=>{const y=20+i*60;return `<rect x="52" y="${y}" width="430" height="38" rx="9" fill="#fff" stroke="${i===labels.length-1?'#9bb7ef':'#cbd3dc'}"/><text x="70" y="${y+24}" font-family="monospace" font-size="11" fill="#202630">${esc(t)}</text>${i<labels.length-1?`<path d="M267 ${y+38}V${y+55}" stroke="#aeb8c4" stroke-width="1.5" marker-end="url(#arrow)"/>`:''}`}).join('')}<text x="560" y="42" font-family="monospace" font-size="9" fill="#77808b">LAPIS / ${esc(slug).toUpperCase()}</text><line x1="560" y1="56" x2="760" y2="56" stroke="#dfe4e9"/><text x="560" y="82" font-family="system-ui" font-size="11" fill="#59616c">Technical abstraction; implementation remains the source of truth.</text></svg>`}
function researchPage(){return page('RESEARCH','RESEARCH','Technical notes for understanding the architecture, tokenizer, training pipeline, evaluation methodology, and current boundaries.')+`<section class="shell section"><div class="researchgrid">${D.research.map(r=>`<a class="docbox" href="${link('research/'+r.slug+'/')}"><span class="idx">TECHNICAL NOTE</span><h3>${esc(r.name)}</h3><p>${esc(r.description)}</p><span class="plain">Open note →</span></a>`).join('')}</div></section>`}
function researchDetail(slug){const r=D.research.find(x=>x.slug===slug);if(!r)return notFound();return `<section class="shell page"><div class="eyebrow">RESEARCH / TECHNICAL NOTE</div><h1>${esc(r.name)}</h1><p class="lede">${esc(r.description)}</p></section><section class="shell section"><article class="paper"><div class="diagram">${diagramSvg(slug)}</div><div class="papergrid"><div><div class="idx">WHAT IS ESTABLISHED</div><h3>${esc(r.name)}</h3><p>${esc(r.description)}</p></div><div><div class="idx">WHAT IS NOT CLAIMED</div><p>Where the repository does not publish a metric, configuration, or maturity level, the site keeps the value explicitly unpublished rather than substituting an estimate.</p><a class="plain" href="https://github.com/sudomarc/LapisLLM" target="_blank" rel="noreferrer">Inspect implementation ↗</a></div></div></article></section>`}
function roadmapPage(){return page('ROADMAP','ROADMAP','Correctness first. Scale second. Roadmap entries carry status and engineering intent instead of invented deadlines.')+`<section class="shell section"><div class="roadmap">${[['01','Foundation','Completed','Repository, configuration, tokenizer, Transformer core, checkpointing, tests, and correctness documentation.'],['02','Validation','In progress','Validation loss, perplexity, tiny-dataset overfitting, deterministic checkpoint round-trip, generation regression, and stronger config validation.'],['03','Small model','Planned','A larger validated configuration, mixed precision, KV cache work, and stronger evaluation.'],['04','Developer API','Planned','A stable serving contract for web apps, tools, clients, and local workflows.'],['05','Scale','Exploring','Distributed training, larger datasets, efficient memory use, and larger open model releases.']].map(x=>`<article class="road"><div class="mono">${x[0]}</div><div><div class="cardtop"><h2>${x[1]}</h2>${pill(x[2])}</div><p>${x[3]}</p></div></article>`).join('')}</div></section>`}
function changelogPage(){return page('CHANGELOG','CHANGELOG','Engineering changes, not marketing headlines. The release history mirrors the repository changelog and keeps validation status visible.')+`<section class="shell section"><div class="releases">${[['0.1.1','2026-09-06','Correctness','Corrected causal target alignment, added padding-aware loss masking, checkpoint state, warmup + cosine scheduling, and expanded tests.'],['0.1.0','2026-09-06','Initial development release','Initial decoder-only Transformer, configurable YAML experiments, tokenizer, data utilities, training/evaluation/generation entry points, and test infrastructure.']].map(x=>`<article class="release"><div class="releasehead"><div><strong>v${x[0]}</strong> ${pill(x[2])}</div><time>${x[1]}</time></div><p>${x[3]}</p></article>`).join('')}</div><div class="links"><a href="https://github.com/sudomarc/LapisLLM/blob/main/CHANGELOG.md" target="_blank" rel="noreferrer">Read source changelog ↗</a></div></section>`}
function aboutPage(){const p=[['Transparency','Publish what is known, measured, and still missing.'],['Reproducibility','Prefer configuration, checkpoints, tests, and documented experiments.'],['Accessibility','Keep development runnable on small hardware while scale matures.'],['Performance','Optimize after correctness and profiling identify the bottleneck.'],['Community','Treat code, docs, skills, and research notes as first-class open-source surfaces.']];return page('ABOUT','ABOUT','Lapis is an engineering project, not a capability claim. The project exists to make modern language-model engineering inspectable enough to study, reproduce, and improve.')+`<section class="shell section"><div class="twocol"><div><div class="idx">THE IDEA</div><h2>Understand the machine by building the machine.</h2></div><div><p>Lapis keeps the core model stack explicit: tokenizer, data pipeline, architecture, training, evaluation, inference, and serving infrastructure.</p><p>The early milestones are intentionally modest. A model that can be inspected and measured is more valuable than a dashboard full of unsupported numbers.</p></div></div></section><section class="shell section"><div class="principles">${p.map((x,i)=>`<article><span>${String(i+1).padStart(2,'0')}</span><h2>${x[0]}</h2><p>${x[1]}</p></article>`).join('')}</div></section><section class="shell section"><div class="headrow"><div><div class="idx">REPOSITORY SIGNALS</div><h2>Live where available.<br><em>Fallbacks stay static.</em></h2></div></div><div class="stats-grid"><div><span>Stars</span><b data-stat="stars">—</b></div><div><span>Forks</span><b data-stat="forks">—</b></div><div><span>Open issues</span><b data-stat="issues">—</b></div><div><span>Latest release</span><b data-stat="release">v0.1.1</b></div></div></section>`}
function skillDetail(slug){const s=D.skills.find(x=>x.slug===slug);if(!s)return notFound();return `<section class="shell page"><div class="eyebrow">SKILL · ${esc(s.category)}</div><h1>${esc(s.name)}</h1><p class="lede">${esc(s.description)}</p></section><section class="shell section"><div class="detailhead">${pill(s.status)}<span class="meta-inline">v${esc(s.version)} · by Lapis</span><a class="plain" href="https://github.com/sudomarc/LapisLLM/blob/main/website/content/skills/${s.slug}/SKILL.md" target="_blank" rel="noreferrer">View SKILL.md ↗</a></div><div class="twocol detailintro"><div><div class="idx">OVERVIEW</div><h2>Designed as a workflow, not a black box.</h2></div><div><p>${esc(s.description)}</p><p>Use the skill when the task is in scope, the required context is available, and the operator can validate the resulting output.</p></div></div><div class="details"><section class="detail"><span class="idx">01</span><h3>Use cases</h3><p>Structured assistance for ${esc(s.name.toLowerCase())}, with explicit evidence and boundaries.</p></section><section class="detail"><span class="idx">02</span><h3>Inputs</h3><pre><code>task: clear objective\nscope: source material or approved target\nconstraints: expected format + limits</code></pre></section><section class="detail"><span class="idx">03</span><h3>Outputs</h3><pre><code>findings: structured observations\nactions: recommended next steps\nlimitations: unresolved uncertainty</code></pre></section><section class="detail"><span class="idx">04</span><h3>Workflow</h3><ol><li>Inspect context and define the task boundary.</li><li>Apply the workflow in explicit stages.</li><li>Separate observed evidence from assumptions.</li><li>Return actionable results and validation notes.</li></ol></section><section class="detail"><span class="idx">05</span><h3>Limitations & safety</h3><p>Experimental package. No implicit access to external systems. Security-oriented workflows require authorization and must not be used to bypass controls or expose secrets.</p></section></div></section>`}
function notFound(){return page('404','NOT FOUND','This path does not exist. The resource may have moved, or the URL may be incorrect.')+`<section class="shell section"><div class="links"><a class="btn primary" href="${link()}">← Back to Lapis</a><a class="plain" href="${link('docs/')}">Open docs →</a></div></section>`}
function route(){if(view==='/')return home();if(view==='/models/')return modelsPage();if(view==='/skills/')return skillsPage();if(view==='/docs/')return docsPage();if(view==='/research/')return researchPage();if(view==='/roadmap/')return roadmapPage();if(view==='/changelog/')return changelogPage();if(view==='/about/')return aboutPage();if(view.startsWith('/skills/'))return skillDetail(view.split('/')[2]);if(view.startsWith('/docs/'))return docDetail(view.split('/')[2]);if(view.startsWith('/research/'))return researchDetail(view.split('/')[2]);return notFound()}
app.innerHTML=shell(route());
const menu=$('[data-menu]'),mobile=$('[data-mobile]');if(menu)menu.onclick=()=>{const open=mobile.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))};
const openSearch=()=>{const root=$('#search-root');const items=[...D.models.map(x=>({t:'Model',n:x.name,d:x.role,h:link('models/')})),...D.skills.map(x=>({t:'Skill',n:x.name,d:x.description,h:link('skills/'+x.slug+'/')})),...D.docs.map(x=>({t:'Docs',n:x.name,d:x.description,h:link('docs/'+x.slug+'/')})),...D.research.map(x=>({t:'Research',n:x.name,d:x.description,h:link('research/'+x.slug+'/')})),{t:'Project',n:'Roadmap',d:'Project status and future work',h:link('roadmap/')},{t:'Project',n:'Changelog',d:'Release history',h:link('changelog/')},{t:'Project',n:'About',d:'Project philosophy',h:link('about/')}];root.innerHTML=`<div class="search-modal"><div class="dialog" role="dialog" aria-modal="true" aria-label="Search Lapis"><div class="searchin"><input autofocus type="search" placeholder="Search Lapis" aria-label="Search Lapis" data-q><span class="mono">Esc</span></div><div class="results" data-results></div><div class="foot"><span>Enter to open</span><span>Esc to close</span></div></div></div>`;const q=$('[data-q]',root),res=$('[data-results]',root);const render=()=>{const term=q.value.toLowerCase().trim(),rows=items.filter(i=>!term||(i.n+' '+i.d+' '+i.t).toLowerCase().includes(term)).slice(0,12);res.innerHTML=rows.length?rows.map(i=>`<a class="result" href="${i.h}"><small>${i.t}</small><div><b>${esc(i.n)}</b><span>${esc(i.d)}</span></div><strong>→</strong></a>`).join(''):`<div class="no-results">No results.</div>`};render();q.oninput=render;q.onkeydown=e=>{if(e.key==='Escape')closeSearch();if(e.key==='Enter')$('.result',root)?.click()};q.focus()};
const closeSearch=()=>{$('#search-root').innerHTML=''};window.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();openSearch()}else if(e.key==='Escape')closeSearch()});document.addEventListener('click',e=>{if(e.target.closest('[data-search-open]'))openSearch()});
document.addEventListener('click',async e=>{const b=e.target.closest('[data-copy]');if(!b)return;const code=b.closest('.codewrap')?.querySelector('code')?.textContent||'';try{await navigator.clipboard.writeText(code);b.textContent='Copied';setTimeout(()=>b.textContent='Copy',1200)}catch{b.textContent='Copy failed';setTimeout(()=>b.textContent='Copy',1200)}});
const ss=$('[data-skill-search]');if(ss){let f='all';const rows=$$('.market>a');const update=()=>{let n=0;rows.forEach(r=>{const show=(f==='all'||r.dataset.skill===f)&&r.dataset.text.includes(ss.value.toLowerCase());r.hidden=!show;n+=show?1:0});const e=$('[data-empty]');if(e)e.hidden=n>0};ss.oninput=update;$$('.filter').forEach(x=>x.onclick=()=>{$$('.filter').forEach(b=>b.classList.remove('on'));x.classList.add('on');f=x.dataset.filter;update()})}
(async()=>{const set=(k,v)=>{const el=document.querySelector(`[data-stat="${k}"]`);if(el)el.textContent=v};try{const r=await fetch('https://api.github.com/repos/sudomarc/LapisLLM',{headers:{Accept:'application/vnd.github+json'}});if(r.ok){const d=await r.json();set('stars',d.stargazers_count);set('forks',d.forks_count);set('issues',d.open_issues_count)}}catch{}try{const r=await fetch('https://api.github.com/repos/sudomarc/LapisLLM/releases/latest',{headers:{Accept:'application/vnd.github+json'}});if(r.ok){const d=await r.json();if(d.tag_name)set('release',d.tag_name)}}catch{}})();
})();