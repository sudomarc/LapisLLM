(() => {
  const D=window.LAPIS_DATA||{};
  const body=document.body, app=document.querySelector("#app");
  const view=body.dataset.view||"/", base=body.dataset.base||"./";
  const link=(p="")=>base+p;
  const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
  const pill=s=>`<span class="pill">${esc(s)}</span>`;
  const nav=[["models/","Models"],["skills/","Skills"],["docs/","Docs"],["research/","Research"],["roadmap/","Roadmap"]];
  const slug=s=>String(s).toLowerCase().replaceAll(" ","-");
  function header(){return `<header class="site-head"><div class="shell nav"><a class="brand" href="${link()}"><span class="mark">L</span><span>Lapis</span><span class="suffix">LLM</span></a><nav class="navlinks" aria-label="Primary">${nav.map(([p,t])=>`<a class="${view.startsWith("/"+p.split("/")[0])?"active":""}" href="${link(p)}">${t}</a>`).join("")}</nav><div class="navright"><button class="search-trigger" type="button" data-search-open>⌘K <span>Search</span></button><a class="git" href="https://github.com/sudomarc/LapisLLM" target="_blank" rel="noreferrer">GitHub ↗</a><button class="menu" type="button" aria-label="Open navigation" aria-expanded="false" data-menu>☰</button></div></div><div class="mobile" data-mobile><div class="shell">${nav.map(([p,t])=>`<a href="${link(p)}">${t}</a>`).join("")}<a href="${link("docs/")}">Get Started</a></div></div></header>`;}
  function footer(){return `<footer class="footer"><div class="shell footgrid"><div><a class="brand" href="${link()}"><span class="mark">L</span><span>Lapis</span><span class="suffix">LLM</span></a><p class="note">Open-source model engineering, documentation, and developer tooling.</p></div><div><span class="label">Explore</span><a href="${link("models/")}">Models</a><a href="${link("skills/")}">Skills</a><a href="${link("docs/")}">Docs</a></div><div><span class="label">Project</span><a href="${link("research/")}">Research</a><a href="${link("roadmap/")}">Roadmap</a><a href="${link("changelog/")}">Changelog</a></div><div><span class="label">Source</span><a href="https://github.com/sudomarc/LapisLLM" target="_blank" rel="noreferrer">GitHub ↗</a><a href="${link("about/")}">About</a></div></div><div class="shell bottom"><span>© 2026 LapisLLM</span><span>Built for inspectability.</span></div></footer>`;}
  const shell=h=>header()+`<main id="main">${h}</main>`+footer()+`<div id="search-root"></div>`;
  const page=(title,kicker,desc)=>`<section class="shell page"><div class="eyebrow">${esc(kicker)}</div><h1>${esc(title)}</h1><p class="lede">${esc(desc)}</p></section>`;
  function home(){return `${page("Open-source reasoning models for developers.",`LAPIS ${D.version} · CORRECTNESS`,"Lapis is a from-scratch decoder-only Transformer project that keeps the model stack visible: tokenizer, data pipeline, training, evaluation, inference, and developer tooling.")}<section class="shell section"><div class="hero-art" role="img" aria-label="Lapis technical pipeline"><div class="grid"></div><div class="node n1"><small>01</small><b>TEXT</b><small>raw input</small></div><div class="line l1"></div><div class="node n2"><small>02</small><b>TOKENS</b><small>ByteLevel BPE</small></div><div class="line l2"></div><div class="core"><b>L</b><span>TRANSFORMER</span></div><div class="line l3"></div><div class="node n3"><small>03</small><b>LOSS</b><small>causal objective</small></div><div class="line l4"></div><div class="node n4"><small>04</small><b>OUTPUT</b><small>next-token logits</small></div><div class="caption"><span>Inspectable path</span><code>data → tokenize → train → evaluate → infer</code></div></div><div class="actions"><a class="btn primary" href="${link("docs/")}">Get Started →</a><a class="btn ghost" href="https://github.com/sudomarc/LapisLLM" target="_blank" rel="noreferrer">View on GitHub ↗</a></div></section><section class="shell section"><div class="twocol"><div class="idx">01 / SYSTEM</div><div><h2>Build the stack.<br><em>Measure what it does.</em></h2><p>Lapis is deliberately small at the current stage. The repository exposes the mechanics of a language model instead of hiding them behind a hosted endpoint or high-level training framework.</p></div></div></section><section class="shell section"><div class="headrow"><div><div class="idx">02 / MODELS</div><h2>One architecture.<br><em>Multiple scales.</em></h2></div><a class="plain" href="${link("models/")}">View all models →</a></div><div class="cards">${(D.models||[]).slice(0,3).map(m=>`<article class="card"><div class="cardtop">${pill(m.status)}<span class="mono">${esc(m.name)}</span></div><h3>${esc(m.name)}</h3><p>${esc(m.role)}</p><div class="mini"><div><span>Config</span><b>${esc(m.config)}</b></div><div><span>Benchmarks</span><b>Not yet published</b></div></div><a class="plain" href="${link("models/"+slug(m.name)+"/")}">View model →</a></article>`).join("")}</div></section><section class="band"><section class="shell section"><div class="headrow"><div><div class="idx">03 / SKILLS</div><h2>A developer layer<br><em>around the model.</em></h2></div><a class="plain" href="${link("skills/")}">Explore skills →</a></div><div class="skilllist">${(D.skills||[]).slice(0,5).map(s=>`<a class="skillrow" href="${link("skills/"+s.slug+"/")}"><span class="mark">${esc(String(s.category||"?").slice(0,1))}</span><span><b>${esc(s.name)}</b><small>${esc(s.description)}</small></span><span class="arrow">→</span></a>`).join("")}</div></section></section><section class="shell section"><div class="twocol"><div><div class="idx">04 / DEVELOPER EXPERIENCE</div><h2>Use Lapis where<br><em>you build.</em></h2></div><div class="dev"><article class="card"><span class="eyebrow">PYTHON</span><h3>Train from the CLI.</h3><p>Use the repository entry points to prepare data, train, evaluate, generate, chat, and serve.</p><code>python scripts/train.py --config configs/tiny.yaml</code></article><article class="card"><span class="eyebrow">LOCAL</span><h3>Keep experiments close.</h3><p>The tiny configuration is designed for development on a small machine; larger training is a future step.</p></article><article class="card"><span class="eyebrow">API</span><h3>Design for integration.</h3><p>A FastAPI layer exists in the repository and is being evolved toward a stable developer-facing contract.</p></article></div></div></section><section class="shell section"><div class="twocol"><div><div class="idx">05 / EVALUATION</div><h2>Evidence before<br><em>capability claims.</em></h2></div><div class="bench"><strong>Not yet benchmarked</strong><p>Numbers will be added only from reproducible experiments.</p><div class="benchgrid">${["MMLU","GSM8K","HumanEval","ARC","TruthfulQA","Tokens/sec"].map(x=>`<span>${x}<b>—</b></span>`).join("")}</div></div></div></section><section class="shell section"><div class="twocol"><div><div class="idx">06 / RESEARCH</div><h2>Document the machine,<br><em>not just the interface.</em></h2></div><div class="researchlist">${(D.research||[]).map((r,i)=>`<a class="researchitem" href="${link("research/"+r.slug+"/")}"><span class="idx">${String(i+1).padStart(2,"0")}</span><div><h2>${esc(r.name)}</h2><p>${esc(r.description)}</p></div><span>→</span></a>`).join("")}</div></div></section><section class="shell section"><div class="twocol"><div><div class="idx">07 / OPEN SOURCE</div><h2>Follow the work<br><em>in the repository.</em></h2></div><div><p>Source, tests, releases, research notes, and the website are developed in the open.</p><a class="btn ghost" href="https://github.com/sudomarc/LapisLLM" target="_blank" rel="noreferrer">Repository ↗</a></div></div></section>`;}
  function modelsPage(){return page("MODEL CATALOG","MODELS","Released, experimental, and planned configurations are shown separately.")+`<section class="shell section"><div class="notice"><strong>Current state</strong>Lapis ${esc(D.version)} is a correctness-focused development release.</div>${(D.models||[]).map(m=>`<article class="modelrow"><div class="modelmain"><div class="cardtop">${pill(m.status)}<span class="mono">${esc(m.name)}</span></div><h2>${esc(m.name)}</h2><p>${esc(m.role)}</p><div class="specs"><div class="spec"><span>Architecture</span><b>${esc(m.architecture)}</b></div><div class="spec"><span>Context</span><b>${m.name==="Lapis Tiny"?"128 tokens":"Not yet published"}</b></div><div class="spec"><span>Parameters</span><b>Not published</b></div><div class="spec"><span>License</span><b>${esc(m.license)}</b></div></div><a class="plain" href="${link("models/"+slug(m.name)+"/")}">Open model page →</a></div><div class="aside"><label>Benchmarks</label><p>Not yet published</p><label>Memory</label><p>Not yet published</p></div></article>`).join("")}</section>`;}
  function modelDetail(id){const m=(D.models||[]).find(x=>slug(x.name)===id);if(!m)return notFound();const tiny=m.name==="Lapis Tiny";return page(m.name,"MODEL CARD",m.role)+`<section class="shell section"><div class="notice"><strong>${esc(m.status)}</strong>${tiny?"Current development configuration.":"Roadmap target; no public release artifact is represented here."}</div><div class="specs model-detail-specs"><div class="spec"><span>Context</span><b>${tiny?"128 tokens":"Not yet published"}</b></div><div class="spec"><span>Parameters</span><b>Not published</b></div><div class="spec"><span>Architecture</span><b>${esc(m.architecture)}</b></div><div class="spec"><span>License</span><b>${esc(m.license)}</b></div><div class="spec"><span>Intended use</span><b>${esc(m.role)}</b></div><div class="spec"><span>Inference</span><b>${tiny?"Python generation/serving scripts":"Not yet published"}</b></div></div><div class="links"><a href="${link("models/")}">← Model catalog</a></div></section>`;}
  function skillsPage(){return page("LAPIS SKILLS","SKILLS","Reusable workflows with explicit purpose, inputs, outputs, workflow, limitations, and safety boundaries.")+`<section class="shell section"><div class="searchbar"><label class="input">⌕<input type="search" placeholder="Search skills" aria-label="Search skills" data-skill-search></label><div class="filters">${["All","Development","Security","Research","Documentation","Data","Evaluation","Productivity"].map((x,i)=>`<button class="filter ${i===0?"on":""}" type="button" data-filter="${x}">${x}</button>`).join("")}</div></div><div class="market">${(D.skills||[]).map(s=>`<a class="skillmarket" data-category="${esc(s.category)}" data-name="${esc((s.name+" "+s.description).toLowerCase())}" href="${link("skills/"+s.slug+"/")}"><div><div class="cardtop">${pill(s.status)}<span class="mono">v${esc(s.version)}</span></div><h2>${esc(s.name)}</h2><p>${esc(s.description)}</p></div><div><span class="label">${esc(s.category)}</span><span>View skill →</span></div></a>`).join("")}</div></section>`;}
  function skillDetail(id){const s=(D.skills||[]).find(x=>x.slug===id);if(!s)return notFound();const contract=(title,value)=>`<section class="content-section"><div class="idx">${title.toUpperCase()}</div>${value?`<div class="prose">${value}</div>`:`<div class="notice"><strong>Not declared in source</strong><p>See the normative package contract in <a href="https://github.com/sudomarc/LapisLLM/blob/main/website/${esc(s.sourcePath||`content/skills/${s.slug}/SKILL.md`)}" target="_blank" rel="noreferrer">SKILL.md ↗</a>.</p></div>`}</section>`;return page(s.name,"LAPIS SKILL",s.description)+`<section class="shell section"><div class="notice"><strong>${pill(s.status)} v${esc(s.version)}</strong><span>${esc(s.category)} · source-backed contract</span></div>${contract("Overview",s.contentHtml)}${contract("Inputs",s.inputs)}${contract("Outputs",s.outputs)}${contract("Workflow",s.workflow)}${contract("Limitations",s.limitations)}${contract("Safety",s.safety)}<section class="content-section"><div class="idx">SOURCE</div><a class="btn ghost" href="https://github.com/sudomarc/LapisLLM/blob/main/website/${esc(s.sourcePath||`content/skills/${s.slug}/SKILL.md`)}" target="_blank" rel="noreferrer">Open SKILL.md ↗</a></section></section>`;}
  function docsPage(){return page("DOCUMENTATION","DOCS","Developer documentation for installation, models, development, training, skills, and reference material.")+`<section class="shell section"><div class="cards">${(D.docs||[]).map(d=>`<a class="card" href="${link("docs/"+d.slug+"/")}"><span class="eyebrow">DOC</span><h2>${esc(d.name)}</h2><p>${esc(d.description)}</p><span class="plain">Read →</span></a>`).join("")}</div></section>`;}
  function docsDetail(id){const d=(D.docs||[]).find(x=>x.slug===id);if(!d)return notFound();return page(d.name,"DOCUMENTATION",d.description)+`<section class="shell section"><article class="prose">${d.contentHtml||`<p>Source content unavailable. <a href="https://github.com/sudomarc/LapisLLM/blob/main/website/${esc(d.sourcePath||`content/docs/${d.slug}.md`)}" target="_blank" rel="noreferrer">Open source ↗</a></p>`}</article><div class="links"><a href="${link("docs/")}">← Documentation</a></div></section>`;}
  function researchPage(){return page("RESEARCH","RESEARCH","Technical notes on architecture, tokenization, training, evaluation, and limitations.")+`<section class="shell section"><div class="market">${(D.research||[]).map(r=>`<a class="skillmarket" href="${link("research/"+r.slug+"/")}"><div><span class="eyebrow">RESEARCH NOTE</span><h2>${esc(r.name)}</h2><p>${esc(r.description)}</p></div><span>Read →</span></a>`).join("")}</div></section>`;}
  function researchDetail(id){const r=(D.research||[]).find(x=>x.slug===id);if(!r)return notFound();return page(r.name,"RESEARCH NOTE",r.description)+`<section class="shell section"><article class="prose">${r.contentHtml||`<p>Source content unavailable.</p>`}</article><div class="links"><a href="${link("research/")}">← Research</a><a href="https://github.com/sudomarc/LapisLLM/blob/main/website/${esc(r.sourcePath||`content/research/${r.slug}.md`)}" target="_blank" rel="noreferrer">Source ↗</a></div></section>`;}
  function roadmap(){return page("ROADMAP","PROJECT ROADMAP","Progress is tracked by engineering state, not promised dates.")+`<section class="shell section"><div class="timeline"><div class="time"><span class="idx">01</span><b>Foundation</b><small>Completed</small></div><div class="time"><span class="idx">02</span><b>Validation</b><small>In progress</small></div><div class="time"><span class="idx">03</span><b>Small model</b><small>Planned</small></div><div class="time"><span class="idx">04</span><b>Scale</b><small>Exploring</small></div></div></section>`;}
  function changelog(){return page("CHANGELOG","RELEASES","Versioned project history, with evidence-based release notes.")+`<section class="shell section"><article class="prose"><h2>0.1.1 — Correctness</h2><p>Correctness tests, checkpoint persistence, warmup + cosine scheduling, corrected target alignment, and explicit limitations.</p><h2>0.1.0 — Initial development release</h2><p>Initial repository structure, decoder-only Transformer, configurable experiments, tokenizer, data utilities, training, inference, API direction, and CI.</p></article></section>`;}
  function aboutPage(){return page("ABOUT LAPIS","ABOUT","An open-source engineering project for understanding language models by building their mechanics in the open.")+`<section class="shell section"><div class="twocol"><div><div class="idx">PHILOSOPHY</div><h2>Transparency.<br><em>Reproducibility.</em></h2></div><div><p>Lapis prioritizes correctness before speed, configuration before hard-coded experiments, and evidence before capability claims.</p></div></div></section>`;}
  function notFound(){return `<section class="shell page notfound"><div class="eyebrow">404 / NOT FOUND</div><h1>This path does not exist.</h1><p class="lede">The requested Lapis route could not be found.</p><div class="actions"><a class="btn primary" href="${link()}">← Back to Lapis</a><a class="btn ghost" href="${link("docs/")}">Read the docs</a></div></section>`;}
  function route(){if(view==="/")return home();if(view==="/models/")return modelsPage();if(view.startsWith("/models/"))return modelDetail(view.split("/")[2]||"");if(view==="/skills/")return skillsPage();if(view.startsWith("/skills/"))return skillDetail(view.split("/")[2]||"");if(view==="/docs/")return docsPage();if(view.startsWith("/docs/"))return docsDetail(view.split("/")[2]||"");if(view==="/research/")return researchPage();if(view.startsWith("/research/"))return researchDetail(view.split("/")[2]||"");if(view==="/roadmap/")return roadmap();if(view==="/changelog/")return changelog();if(view==="/about/")return aboutPage();return notFound();}
  if(!app)return;
  app.innerHTML=shell(route());
  const menu=document.querySelector("[data-menu]"), mobile=document.querySelector("[data-mobile]");
  if(menu&&mobile)menu.addEventListener("click",()=>{const open=mobile.classList.toggle("open");menu.setAttribute("aria-expanded",String(open));});
  const search=(open=true)=>{const root=document.querySelector("#search-root");if(!root)return;if(!open){root.innerHTML="";return;}const items=[...(D.models||[]).map(x=>({t:x.name,d:x.role,u:link("models/"+slug(x.name)+"/")})),...(D.skills||[]).map(x=>({t:x.name,d:x.description,u:link("skills/"+x.slug+"/")})),...(D.docs||[]).map(x=>({t:x.name,d:x.description,u:link("docs/"+x.slug+"/")})),...(D.research||[]).map(x=>({t:x.name,d:x.description,u:link("research/"+x.slug+"/")}))];root.innerHTML=`<div class="search-modal" role="dialog" aria-modal="true" aria-label="Search"><div class="search-dialog"><button class="search-close" type="button" data-search-close aria-label="Close search">×</button><input autofocus type="search" placeholder="Search Lapis" aria-label="Search Lapis" data-global-search><div class="search-results" data-results>${items.map(x=>`<a href="${x.u}"><b>${esc(x.t)}</b><span>${esc(x.d)}</span></a>`).join("")}</div></div></div>`;const input=root.querySelector("[data-global-search]"),results=root.querySelector("[data-results]");input.addEventListener("input",()=>{const q=input.value.toLowerCase().trim();results.innerHTML=items.filter(x=>(x.t+" "+x.d).toLowerCase().includes(q)).map(x=>`<a href="${x.u}"><b>${esc(x.t)}</b><span>${esc(x.d)}</span></a>`).join("")||`<div class="notice"><strong>No results</strong></div>`;});root.querySelector("[data-search-close]").addEventListener("click",()=>search(false));};
  document.querySelector("[data-search-open]")?.addEventListener("click",()=>search());
  document.addEventListener("keydown",e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();search();}if(e.key==="Escape")search(false);});
  document.querySelectorAll("[data-filter]").forEach(btn=>btn.addEventListener("click",()=>{document.querySelectorAll("[data-filter]").forEach(b=>b.classList.remove("on"));btn.classList.add("on");const filter=btn.dataset.filter;document.querySelectorAll(".skillmarket").forEach(card=>{card.hidden=filter!=="All"&&card.dataset.category!==filter;});document.querySelector("[data-skill-search]")?.dispatchEvent(new Event("input"));}));
  document.querySelector("[data-skill-search]")?.addEventListener("input",e=>{const q=e.target.value.toLowerCase().trim(),filter=document.querySelector("[data-filter].on")?.dataset.filter||"All";document.querySelectorAll(".skillmarket").forEach(card=>{card.hidden=!(!q||card.dataset.name.includes(q))||!(filter==="All"||card.dataset.category===filter);});});
})();
